import React, { useEffect, useState } from 'react'
import { getAll } from '../modules/APICalls'

export const List = () => {
    return (
        <>
            <h1>Christmas List</h1>
        </>
    )
}